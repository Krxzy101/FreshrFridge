# Leave unset to use Vite proxy (/api → localhost:3000). Recommended for dev.
# VITE_API_BASE=/api

# Only if you run without Vite proxy (e.g. production static build + separate API):
# VITE_API_BASE=http://localhost:3000
